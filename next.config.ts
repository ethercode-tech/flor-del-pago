import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "images.unsplash.com",
      },
    ],
  },
  async redirects() {
    return [
      {
        source: "/chef",
        destination: "/daniel-hansen",
        permanent: true,
      },
    ];
  },
};

export default nextConfig;
